spring:
  datasource:
    url: jdbc:postgresql://postgres:5432/booking_db
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update

event-service:
  url: http://event-service:8081
